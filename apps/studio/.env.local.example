# apps/studio needs the ANON key, never the service role key.
# The service role key must never reach a browser bundle.
NEXT_PUBLIC_SUPABASE_URL=https://pdficpdfrituqjzaleen.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Where the magic link returns to. Local dev uses http://localhost:3100
NEXT_PUBLIC_SITE_URL=http://localhost:3100
